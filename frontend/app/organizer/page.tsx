'use client';
import {useEffect,useState} from 'react';
import {api} from '../../lib/api';
export default function Organizer(){const [events,setEvents]=useState<any[]>([]);const [catalog,setCatalog]=useState<any[]>([]);const [form,setForm]=useState({title:'',description:'',event_type:'seated',starts_at:'',location:'',capacity:40,price_cents:3500,published:true,external_source:'tmdb',external_id:''});const [q,setQ]=useState('cinema');const [error,setError]=useState('');async function load(){try{setEvents(await api('/organizer/events'))}catch(e:any){setError(e.message)}}useEffect(()=>{load()},[]);async function search(){try{const r=await api(`/external/catalog?source=tmdb&q=${encodeURIComponent(q)}`);setCatalog(r.items)}catch(e:any){setError(e.message)}}async function create(e:any){e.preventDefault();try{await api('/organizer/events',{method:'POST',body:JSON.stringify(form)});setForm({...form,title:'',description:'',external_id:''});load()}catch(e:any){setError(e.message)}}return <><div className="hero"><div><div className="eyebrow">Back office</div><h1>Organizador.</h1><p className="muted">Catálogo externo + publicação + gestão de capacidade.</p></div><div className="card"><div className="big-stat">{events.length}</div><p className="muted">eventos próprios</p></div></div>{error&&<div className="status bad" style={{marginBottom:16}}>{error}</div>}<div className="grid"><div className="card"><h2>Encontrar no TMDb</h2><div className="row"><input value={q} onChange={e=>setQ(e.target.value)}/><button className="btn ghost" onClick={search}>Buscar</button></div><div style={{marginTop:14,display:'grid',gap:8}}>{catalog.map(c=><button key={c.id} className="btn ghost" style={{justifyContent:'flex-start'}} onClick={()=>setForm(f=>({...f,title:c.title,external_id:c.id}))}>{c.title}</button>)}</div></div><div className="card"><h2>Novo evento</h2><form className="form" onSubmit={create}><label>Título<input value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></label><label>Descrição<textarea value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><label>Data/hora<input type="datetime-local" value={form.starts_at} onChange={e=>setForm({...form,starts_at:new Date(e.target.value).toISOString()})}/></label><label>Local<input value={form.location} onChange={e=>setForm({...form,location:e.target.value})}/></label><div className="grid"><label>Capacidade<input type="number" value={form.capacity} onChange={e=>setForm({...form,capacity:Number(e.target.value)})}/></label><label>Preço (centavos)<input type="number" value={form.price_cents} onChange={e=>setForm({...form,price_cents:Number(e.target.value)})}/></label></div><label>ID externo<input value={form.external_id} onChange={e=>setForm({...form,external_id:e.target.value})}/></label><button className="btn accent">Publicar evento</button></form></div></div><div style={{marginTop:18}} className="grid">{events.map(e=><div className="card" key={e.id}><span className="pill">{e.published?'publicado':'rascunho'}</span><h3>{e.title}</h3><p className="muted">{e.location} · R$ {(e.price_cents/100).toFixed(2)}</p></div>)}</div></>}
